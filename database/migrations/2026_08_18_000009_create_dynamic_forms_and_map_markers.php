<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\Schema;
return new class extends Migration {public function up():void{Schema::create('dynamic_forms',function(Blueprint $t){$t->id();$t->foreignId('category_id')->nullable()->constrained()->nullOnDelete();$t->string('name');$t->json('fields');$t->boolean('is_active')->default(true);$t->timestamps();});Schema::create('map_markers',function(Blueprint $t){$t->id();$t->string('title');$t->string('type')->default('service_area');$t->decimal('latitude',10,7);$t->decimal('longitude',10,7);$t->string('icon')->nullable();$t->json('meta')->nullable();$t->boolean('is_active')->default(true);$t->timestamps();});}public function down():void{Schema::dropIfExists('map_markers');Schema::dropIfExists('dynamic_forms');}};
