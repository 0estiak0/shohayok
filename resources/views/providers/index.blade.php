@extends('layouts.app', ['title' => 'Service Providers — Shohayok'])
@section('content')
<div class="container py-5"><h2 class="fw-bold">Service Providers</h2><p class="text-secondary mb-4">Browse all service providers.</p><div class="row g-4">@forelse($providers as $provider)<div class="col-sm-6 col-lg-4 col-xl-3"><div class="card-soft p-4 text-center h-100 position-relative"><div class="provider-avatar mx-auto">@if($provider->avatar)<img src="{{ asset('storage/'.$provider->avatar) }}" alt="{{ $provider->name }}">@else 👨‍🔧 @endif</div><a class="stretched-link text-dark" href="{{ route('providers.show',$provider) }}"><h5 class="mt-3">{{ $provider->name }}</h5></a><div class="text-warning">★ {{ number_format($provider->reviews_avg_rating ?: 0,1) }}</div><small class="text-secondary">{{ $provider->reviews_count }} reviews</small></div></div>@empty<div class="col-12 text-center text-secondary py-5">No providers found.</div>@endforelse</div><div class="mt-4">{{ $providers->links() }}</div></div>
@endsection
