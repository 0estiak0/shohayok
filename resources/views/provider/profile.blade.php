@extends('layouts.app', ['title' => 'My Profile — Shohayok'])
@section('content')
<div class="container-fluid"><div class="row">@include('provider.partials.sidebar')<main class="col-lg-10 dashboard-main p-4 p-xl-5"><div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="fw-bold">My Profile</h2><p class="text-secondary">Your public provider information.</p></div><a class="btn btn-primary" href="{{ route('provider.profile.edit') }}">Edit Profile</a></div><div class="card-soft p-4"><div class="d-flex gap-4 align-items-center mb-4"><div class="provider-avatar">@if($u->avatar)<img src="{{ asset('storage/'.$u->avatar) }}" alt="{{ $u->name }}">@else 👨‍🔧 @endif</div><div><h3>{{ $u->name }}</h3><span class="badge badge-verified">{{ $u->is_verified?'✓ Verified Provider':'Verification Pending' }}</span></div></div><div class="row g-3"><div class="col-md-6"><b>Email</b><div class="text-secondary">{{ $u->email }}</div></div><div class="col-md-6"><b>Phone</b><div class="text-secondary">{{ $u->country_code }} {{ $u->phone ?: 'Not provided' }}</div></div><div class="col-md-6"><b>Address</b><div class="text-secondary">{{ $u->address ?: 'Not provided' }}</div></div><div class="col-md-6"><b>Location</b><div class="text-secondary">{{ collect([$u->city,$u->country])->filter()->join(', ') ?: 'Not provided' }}</div></div><div class="col-md-6"><b>Availability</b><div class="text-secondary">{{ ucfirst($u->availability_status) }}</div></div></div></div></main></div></div>
@endsection
