@extends('layouts.app', ['title' => 'Provider Settings — Shohayok'])
@section('content')
<div class="container-fluid"><div class="row">@include('provider.partials.sidebar')<main class="col-lg-10 dashboard-main p-4 p-xl-5"><h2 class="fw-bold">Settings</h2><p class="text-secondary mb-4">Control your provider availability.</p><div class="card-soft p-4" style="max-width:700px"><form method="post" action="{{ route('provider.settings.update') }}">@csrf @method('PATCH')<label class="form-label">Availability status</label><select class="form-select mb-3" name="availability_status">@foreach(['available'=>'Available — accepting requests','busy'=>'Busy — limited availability','inactive'=>'Inactive — hide availability'] as $value=>$label)<option value="{{ $value }}" @selected($u->availability_status === $value)>{{ $label }}</option>@endforeach</select><button class="btn btn-primary">Save Settings</button></form><hr><a href="{{ route('provider.profile.edit') }}">Edit personal profile information →</a></div></main></div></div>
@endsection
