@extends('layouts.app', ['title' => 'How It Works — Shohayok'])
@section('content')
<div class="container py-5"><div class="text-center mb-5"><h1 class="fw-bold">How Shohayok Works</h1><p class="text-secondary">Find trusted services and rentals in a few simple steps.</p></div><div class="row g-4 mb-5">@foreach([['1','Search','Browse service providers or rental listings by category and location.'],['2','Compare','Review listing details, pricing, discounts, provider profiles, and ratings.'],['3','Contact or Book','Message the provider directly or submit a booking request.'],['4','Get It Done','Confirm the schedule, receive the service, and leave a helpful review.']] as [$number,$title,$description])<div class="col-md-6 col-xl-3"><div class="card-soft p-4 h-100 text-center"><div class="icon-tile mx-auto fs-4 fw-bold">{{ $number }}</div><h4 class="mt-3">{{ $title }}</h4><p class="text-secondary mb-0">{{ $description }}</p></div></div>@endforeach</div><div class="card-soft p-4 p-lg-5"><div class="row align-items-center g-4"><div class="col-lg-8"><h3>Are you a service provider?</h3><p class="text-secondary mb-0">Create a provider account, publish your services, showcase recent work, receive bookings, and communicate with customers.</p></div><div class="col-lg-4 text-lg-end">@auth<a class="btn btn-primary" href="{{ auth()->user()->isProvider()?route('provider.dashboard'):route('provider.apply') }}">{{ auth()->user()->isProvider()?'Open Dashboard':'Become a Provider' }}</a>@else<a class="btn btn-primary" href="{{ route('register') }}">Create an Account</a>@endauth</div></div></div></div>
@endsection
