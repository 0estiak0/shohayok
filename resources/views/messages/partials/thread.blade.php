<div class="card-soft p-4"><div style="min-height:320px;max-height:520px;overflow:auto">@forelse($messages as $message)@php($mine = $message->sender_id === auth()->id())<div class="d-flex {{ $mine ? 'justify-content-end' : 'justify-content-start' }} mb-3"><div class="p-3 rounded-4 {{ $mine ? 'bg-primary text-white' : 'bg-light' }}" style="max-width:75%"><div class="small fw-semibold mb-1">{{ $mine ? 'You' : $otherUser->name }}</div>{{ $message->message }}<div class="small opacity-75 mt-1">{{ $message->created_at->format('d M Y, h:i A') }}</div></div></div>@empty<div class="text-center text-secondary py-5">No messages yet. Start the conversation below.</div>@endforelse</div><form method="post" action="{{ $formAction }}" class="d-flex gap-2 border-top pt-3">@csrf<textarea class="form-control" name="message" rows="2" maxlength="3000" required placeholder="Write a message..."></textarea><button class="btn btn-primary px-4">Send</button></form></div>
